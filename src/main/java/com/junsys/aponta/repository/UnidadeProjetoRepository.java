package com.junsys.aponta.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.junsys.aponta.model.UnidadeProjeto;

public interface UnidadeProjetoRepository extends JpaRepository<UnidadeProjeto, Long> {
}